JWT_SECRET=replace_with_strong_secret
JWT_EXPIRES_IN=7d

# ── PostgreSQL ────────────────────────────────────────────────────────────────
# Option A: full connection string (recommended for hosted DBs)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/startiqo?sslmode=require

# Option B: individual variables (used when DATABASE_URL is not set)
PGHOST=localhost
PGPORT=5432
PGDATABASE=startiqo
PGUSER=postgres
PGPASSWORD=Dhanas@1205

# Set to 'true' if your hosted provider requires SSL
DATABASE_SSL=true

